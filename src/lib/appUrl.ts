/** URL pública de la app en GitHub Pages (siempre para el QR) */
export const APP_PUBLIC_URL = 'https://miguellov.github.io/access-control-wallpaper';

export function getAppUrl(): string {
  return APP_PUBLIC_URL;
}
